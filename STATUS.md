# Краткая сверка с ТЗ

23.09.2026 · проверен `main@b676851`. Исходная таблица замечаний частично устарела.
Pipeline запущен на `data/` с результатами во временной папке; рабочий `output/` не изменён.

| Требование | Результат проверки |
| --- | --- |
| Один запуск от Parquet до CSV | Работает: `python src/pipeline.py --data data --output-dir output`. |
| Узлы, схемы, роли | 2 248 уникальных GID; сумма `n_nodes` = 2 248; схемы трёх CSV соответствуют ТЗ; присутствуют все шесть ролей. |
| Оценки 0–1 | `role_score`: 0.412–1.000; `priority_score`: 0.100–0.965. Прежняя шкала 0–100 исправлена. |
| Evidence ≤ 200 символов | Максимум 189; прежнее превышение исправлено. |
| Граница depth=4 | Все 444 пограничных узла имеют флаг ограничения; terminal им не присвоен. |
| UI | По коду есть поиск строкового GID, карточка, направленные связи, роль, кластер, Top Nodes и оговорки. |
| README и зависимости | Корневой README обновлён; установка описана через `requirements-test.txt`, включающий UI, NetworkX, NumPy и SciPy. |

## Что остаётся проверить или исправить

- **Seed:** в метриках `pass_through` пустой, но `src/roles.py` повторно вычисляет отношение сумм для всех узлов без исключения seed. Это остаётся риском интерпретации ролей.
- **Louvain:** взвешенная неориентированная проекция есть, однако реализована только фаза локальных перемещений, без многоуровневого объединения сообществ. Полное соответствие требованию Louvain не подтверждено.
- **Ветки:** `main` содержит полный запуск и кластеризацию. `analytics@667beef` принимает только метрики и назначает отдельный кластер каждому GID; для финального интегрированного запуска использовать `main`.

Проверены код и отдельный запуск pipeline в существующем окружении. Полный набор тестов и установка в чистом окружении в этой сверке не запускались.
